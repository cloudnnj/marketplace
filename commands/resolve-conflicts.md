---
title: "Resolve Merge Conflicts"
description: "Detects the PR associated with the current branch, identifies all merge conflicts with the target branch, and automatically resolves them using intelligent conflict resolution strategies."
category: "Git Operations"
tags: ["git", "merge", "conflicts", "pr", "automation"]
version: "1.0"
model: "opus"
examples:
  - "/resolve-conflicts <description>"
---

# Resolve Merge Conflicts

Detects the PR associated with the current branch, identifies all merge conflicts with the target branch, and automatically resolves them using intelligent conflict resolution strategies.

## Usage

`/resolve-conflicts <description>`

## What This Command Does

This command helps with detects the pr associated with the current branch, identifies all merge conflicts with the target branch, and automatically resolves them using intelligent conflict resolution strategies..

## Workflow

1. Analyze the request
2. Plan the approach
3. Execute the necessary steps
4. Verify the results

---

## GitHub API via MCP

Use the **GitHub MCP tools** for all GitHub interactions. Never use the `gh` CLI or raw `curl` API calls.

### Finding the PR for the Current Branch

Use `mcp__github__list_pull_requests` with `owner`, `repo`, `head: "<owner>:<branch>"`, `state: "open"`.

### Reading PR Details

- **PR metadata**: `mcp__github__pull_request_read` with `method: "get"`, `owner`, `repo`, `pullNumber`
- **PR diff**: `mcp__github__pull_request_read` with `method: "get_diff"`, `owner`, `repo`, `pullNumber`

---
*Generated by CrewDeck*
